package fluid_modes;

/**
 * creates a perturbation mode with 'm' blobs
 *
 */
public class Mode {
	
	private int mode_number;
	private Blob[] blobs;
	
	public Mode(int m) {
		this.mode_number = m;
		
		blobs = new Blob[m];
		for (int i = 0; i < m; i++) {
			blobs[i] = new Blob(x, y)
		}
	}
}
